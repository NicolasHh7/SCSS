# 📘 Formação Web Developer: Fundamentos e Evolução

Este repositório é um compêndio técnico que documenta minha jornada de aprendizado, cobrindo desde a marcação essencial com **HTML**, a estilização avançada com **SCSS**, até a lógica tipada com **TypeScript**.

<p align="left">
  <img src="https://skillicons.dev/icons?i=git,github,html,css,sass" />
</p>


---

## 🌐 1. HTML (A Estrutura)

O HTML é a linguagem de marcação que define o esqueleto de qualquer site.

* **Conceito:** Responsável pela estrutura e semântica.
* **HTML5:** Introduziu a **Web Semântica**, tornando o código compreensível para máquinas e motores de busca.
* **Anatomia:** Composto por tags de abertura (`<p>`), fechamento (`</p>`) e atributos (`src`, `href`).
* **Estrutura Básica:** Todo documento requer `<!DOCTYPE html>`, `<html>`, `<head>` e `<body>`.

---

## 🎨 2. SCSS/Sass (A Estilização)

O SCSS é um pré-processador que eleva o CSS a um nível de linguagem de programação, permitindo maior organização.

* **Instalação:** `npm install -g sass`
* **Vantagens:** Uso de variáveis (`$`), mixins (`@mixin`) e aninhamento de seletores.
* **Modularização:** Uso de arquivos parciais (`_arquivo.scss`) para separar responsabilidades.
* **Automação:** Modo **Watch** (`sass --watch`) para compilação automática em tempo real.

---

## 📘 3. TypeScript (A Lógica)

O TypeScript adiciona **tipagem estática** ao JavaScript, garantindo segurança e escalabilidade ao código.

* **Instalação:** `npm i typescript -D`
* **Destaques:** Tipagem de parâmetros, detecção de erros em tempo de compilação e suporte a ES Modules.
* **Configuração:** Gerenciado pelo arquivo `tsconfig.json`.
* **Compilação:** O comando `npx tsc` transforma o código TS em JS legível pelo navegador.

---

## 🛠️ Ferramentas e Boas Práticas

Para manter este projeto em alto nível, as seguintes ferramentas foram integradas:

* **Prettier:** Formatação automática para garantir a legibilidade.
* **DevTools:** Uso intensivo do console para depuração, inspeção de elementos e análise de performance.
* **Versionamento:** Git para controle de histórico e evolução do código.

---

## 🧠 Resumo de Conceitos e Arquivos

| Tecnologia | Foco Principal | Ferramenta de Compilação |
| :--- | :--- | :--- |
| **HTML** | Estrutura e Semântica | Nativo (Navegador) |
| **SCSS** | Estilo e Reuso | `sass --watch` |
| **TypeScript** | Tipagem e Segurança | `tsc -w` |

> "A eficiência é a aliada da curiosidade; quanto menos tempo gastamos com processos, mais tempo temos para as descobertas."

---

## 📂 Conteúdo Prático
* **Const-obj / Types / Interfaces** (TypeScript)
* **Variáveis / Mixins / BEM** (SCSS)
* **Semântica / Tags / DevTools** (HTML)
